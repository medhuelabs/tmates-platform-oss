## tmates-platform environment template
# Copy to `.env.dev` for local work or `.env.prod` for deployments.
# Never commit files containing real secrets.

# ---------------------------------------------------------------------------
# Core runtime
# ---------------------------------------------------------------------------
ENV=dev                             # Use "prod" when deploying
USER_DISPLAY_NAME=Automation Lead   # Friendly name agents can reference
USER_EMAIL=                         # Optional email for summaries/notifications
INSTALLED_AGENTS=adam
AGENT_CATALOG_ENABLED=0             # 0=use app/agents/<key>/ locally, 1=download bundles from Supabase
AGENT_CATALOG_ENV=dev               # dev|staging|prod rollout bucket when catalog is enabled

# ---------------------------------------------------------------------------
# Security
# ---------------------------------------------------------------------------
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=replace-with-32-byte-base64-key
SESSION_SECRET=replace-with-random-string
# Optional: override the file view token secret (defaults to SESSION_SECRET)
# FILE_VIEW_TOKEN_SECRET=replace-with-random-hmac-secret

# ---------------------------------------------------------------------------
# Optional local overrides
# ---------------------------------------------------------------------------
USER_ID=demo-dev-user

# ---------------------------------------------------------------------------
# Supabase project credentials (required for all environments)
# ---------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret
SUPABASE_DB_PASSWORD=your-supabase-db-password
SUPABASE_STORAGE_BUCKET=user-files
SUPABASE_STORAGE_PREFIX=users
SUPABASE_SIGNED_URL_TTL=3600
DATABASE_URL=postgresql://username:password@hostname:5432/database

# ---------------------------------------------------------------------------
# File storage backend (choose one)
# ---------------------------------------------------------------------------
# local  -> store files on disk under files/users/<id>
# supabase -> use Supabase Storage (requires SUPABASE_* settings above)
# s3     -> use AWS S3 or any S3-compatible bucket
FILE_STORAGE_BACKEND=local

# When using S3-compatible storage
S3_BUCKET_NAME=
S3_STORAGE_PREFIX=users
S3_SIGNED_URL_TTL=3600
AWS_REGION=
AWS_PROFILE=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
S3_ENDPOINT_URL=
S3_FORCE_PATH_STYLE=0

# ---------------------------------------------------------------------------
# Billing & Stripe
# ---------------------------------------------------------------------------
STRIPE_BILLING_ENABLED=0
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CHECKOUT_SUCCESS_URL=
STRIPE_CHECKOUT_CANCEL_URL=
STRIPE_PORTAL_RETURN_URL=

# ---------------------------------------------------------------------------
# LLM providers
# ---------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-5-mini
# Uncomment to force a specific client when multiple providers are configured
# OPENAI_CLIENT=openai

# Azure OpenAI (optional)
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-azure-openai-key
# AZURE_OPENAI_API_VERSION=2025-03-01-preview
# AZURE_OPENAI_DEPLOYMENT=your-azure-deployment-name

# Optional: Google Generative AI / Imagen (used if you add media agents)
GEMINI_API_KEY=

# ---------------------------------------------------------------------------
# Logging & telemetry
# ---------------------------------------------------------------------------
SUPPRESS_SYSTEM_LOGS=1
SYSTEM_LOG_PREFIXES=
VERBOSE=1
APPEND_BODY=1
LOG_LEVEL=INFO
LOG_RUNS=0

# ---------------------------------------------------------------------------
# Agent-specific configuration
# ---------------------------------------------------------------------------
# Individual agents load secrets from their own .env files.
# Copy the relevant files to app/agents/<agent>/.env with your credentials.
